<section id="primary_content" class="container">
	

	<div id="schemantics" class="panel">
	
		<h1>Events</h1>
		
		<div class="abstract">
			<p class="cta core_action"><%= link_to "Tell us about an event", "mailto:hello@leagueofintrapreneurs.com?subject=New intrapreneurial event" %></p>
			<p>Intrapreneurs are tackling a wide array of issues &ndash; from health to poverty to environment. Some are working locally, others globally. Here we feature events that are likely to inspire and connect intrapreneurs.</p>
		</div>
				
		<% if @events.any? %>
			<% @events.each do |event| %>
		
				<article class="event">
					<time>
						<span><%= event.date.strftime('%B') %></span>
						
						<%= event.date.strftime('%A') %> <%= event.date.day.ordinalize %>
					</time>
					<div class="content">
						<h2><%= link_to event.title, event.url %></h2>
						<%= simple_format event.body %>
					</div>
				</article>
			
			<% end %>
		<% else %>
			<p>We're not aware of any events coming up, <%= link_to "let us know" %> if you have one to share.</p>
		<% end %>
	</div>
		
	<aside>
		
		<% if @posts.any? %>		
		<ul class="content_grid">
		<% @posts.each do |post| %>
			<%= render :partial => "posts/block", :locals => {:post=>post} %>
		<% end %>
		</ul>
		<% end %>
		
	</aside>

</section>